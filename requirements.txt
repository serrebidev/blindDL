wxPython>=4.2; platform_system != "Linux"
# Speaks status-bar messages through the running screen reader, and puts
# them on a Braille display. Optional at runtime: blindDL falls back to
# NVDA's and JAWS' own APIs when it is absent.
accessible-output2>=0.17; platform_system == "Windows"
# The updater keeps yt-dlp on the nightly (pre-release) builds; this is
# just the baseline for a fresh install.
yt-dlp[default]>=2025.1.1
-r requirements-musicdl.txt
# Side B is vendored in ./sideb -- its upstream repository was deleted in
# August 2026, so there is nothing left to install from. These are the
# dependencies its code imports; see THIRD_PARTY_NOTICES.md.
httpx>=0.27
pydantic>=2.7
pydantic-settings>=2.3
questionary>=2.0
rich>=14.3.3,<15
ytmusicapi>=1.11.5,<2
# Free audiobook sites (LibriVox, LoyalBooks and friends). The git build
# carries the chapter/narrator fields the Search tab shows; PyPI's 0.7 does
# not.
audiobooker @ git+https://github.com/LeMetadatarr/audiobooker
requests>=2.32.5,<3
defusedxml>=0.7.1
# Optional: the in-app torrent engine (Settings, Torrents). blindDL runs
# without it and hands magnets to the user's own client instead, so the
# marker keeps a fresh install from failing on a Python version libtorrent
# has not published wheels for yet. Settings offers to install it on demand.
libtorrent>=2.0.11; python_version < "3.14"
mutagen>=1.47,<2
pycryptodome>=3.23,<4
platformdirs>=4.9.4,<5
tzdata>=2025.2
# Optional at runtime through Settings, but included in complete installs so
# enabling Soulseek never requires a separate package-install step.
aioslsk>=1.6.3,<2
python-vlc>=3.0.21203
# Avoid known PyInstaller hook warnings in newer mypyc/packaging layouts.
chardet>=7,<7.4
pycparser>=2.23,<3
-r requirements-adult.txt
